<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weihua.infrastructure.dao.IPortfolioSnapshotDao">

    <resultMap id="BaseResultMap" type="com.weihua.infrastructure.dao.po.PortfolioSnapshotPO">
        <id column="id" property="id" />
        <result column="strategy_instance_id" property="strategyInstanceId" />
        <result column="decision_log_id" property="decisionLogId" />
        <result column="total_balance" property="totalBalance" />
        <result column="available_balance" property="availableBalance" />
        <result column="positions_json" property="positionsJson" />
        <result column="recorded_at" property="recordedAt" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <sql id="Base_Column_List">
        id, strategy_instance_id, decision_log_id, total_balance, available_balance, positions_json, recorded_at, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.weihua.infrastructure.dao.po.PortfolioSnapshotPO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_portfolio_snapshots (
            strategy_instance_id, decision_log_id, total_balance, available_balance, positions_json, recorded_at, create_time, update_time
        ) VALUES (
            #{strategyInstanceId}, #{decisionLogId}, #{totalBalance}, #{availableBalance}, #{positionsJson}, #{recordedAt}, NOW(), NOW()
        )
    </insert>

    <select id="selectByStrategyId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM t_portfolio_snapshots
        WHERE strategy_instance_id = #{strategyInstanceId}
        ORDER BY recorded_at DESC
    </select>

</mapper>
